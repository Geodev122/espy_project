specVersion: "v1beta"
serviceId: "espy-protocol"
location: "us-central1"
schema:
  source: "."
  datasource:
    postgresql:
      cloudSql:
        instanceId: "espy-instance"
      database: "espy_db"
connectors:
  - "./connector"
