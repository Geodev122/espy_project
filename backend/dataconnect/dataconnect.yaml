specVersion: "v1alpha"
serviceId: "espy-protocol"
location: "us-central1"
schema:
  source: "."
  datasource:
    postgresql:
      database: "espy_db"
      cloudSql:
        instanceId: "espy-instance"
connector:
  - id: "espy-connector"
    path: "./connector"
